# SaranyaMart local configuration template.
# Copy this file to src/main/resources/config.properties (gitignored) and edit.
# Environment variables (see .env.example) take precedence over these values.
jdbc.url=jdbc:h2:file:./data/saranyamart
jdbc.username=sa
jdbc.password=
db.pool.size=10
db.reset.on.startup=false
ai.chatbot.provider=mock
