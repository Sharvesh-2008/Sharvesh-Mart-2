package com.sharvesh.sharvesh_mart.model;

/**
 * User roles supported by the marketplace.
 * Admin accounts are provisioned via the seed script only (requirement F1).
 */
public enum Role {
    BUYER,
    SELLER,
    ADMIN
}
